$(document).ready(function() {
	// $('#fullpage').fullpage({
	// 	// navigation: true,
	// 	// navigationPosition: 'right',
	// 	// navigationTooltips: ['First page', 'Second page', 'Third and last page']
	// });
	$('.slick-benefits').slick({
		prevArrow: $('#benefits .slick-prev'),
		nextArrow: $('#benefits .slick-next'),
	});
});